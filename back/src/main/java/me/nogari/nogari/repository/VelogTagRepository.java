package me.nogari.nogari.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import me.nogari.nogari.entity.VelogTag;

public interface VelogTagRepository extends JpaRepository<VelogTag, Long> {
}
